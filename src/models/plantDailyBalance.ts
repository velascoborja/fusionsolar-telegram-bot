export class PlantDailyBalance {
    exported: number
    imported: number

    constructor(exported: number, imported: number) {
        this.exported = exported
        this.imported = imported
    }
}